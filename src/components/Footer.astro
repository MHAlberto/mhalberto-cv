---
title: "Footer"

// Información para el footer
const footerInfo = {
    email: "mario@example.com",
    github: "https://github.com/mario",
    linkedin: "https://linkedin.com/in/mario",
    website: "https://mario-portfolio.com",
    rights: "© 2025 Mario C. Todos los derechos reservados."
};
---

<footer class="footer">
    <div class="container">
        
        <!-- Grid de Secciones -->
        <div class="footer-grid">
            
            <!-- Sección Contacto -->
            <div class="footer-section">
                <h3>Contacto</h3>
                <div class="footer-content">
                    <a href={`mailto:${footerInfo.email}`} class="contact-link">{footerInfo.email}</a>
                </div>
            </div>

            <!-- Sección Links Rápidos -->
            <div class="footer-section">
                <h3>Navegación</h3>
                <ul class="footer-links">
                    <li><a href="/">Inicio</a></li>
                    <li><a href="/sobre">Sobre Mí</a></li>
                    <li><a href="/proyectos">Proyectos</a></li>
                    <li><a href="/contacto">Contacto</a></li>
                </ul>
            </div>

            <!-- Sección Redes Sociales -->
            <div class="footer-section">
                <h3>Redes Sociales</h3>
                <ul class="footer-links">
                    <li><a href={footerInfo.github} target="_blank" rel="noopener noreferrer">GitHub</a></li>
                    <li><a href={footerInfo.linkedin} target="_blank" rel="noopener noreferrer">LinkedIn</a></li>
                    <li><a href={footerInfo.website} target="_blank" rel="noopener noreferrer">Website</a></li>
                </ul>
            </div>

            <!-- Botón CTA -->
            <div class="footer-section footer-cta">
                <a href="/cv" class="btn-footer">Ver Currículum</a>
            </div>

        </div>

        <!-- Separador y Derechos -->
        <div class="footer-bottom">
            <div class="footer-rights">
                {footerInfo.rights}
            </div>
        </div>

    </div>
</footer>

<style>
    /* --- Variables --- */
    :root {
        --bg-color: #ffffff;
        --text-color: #111111;
        --accent-color: #666666;
        --border-color: #eeeeee;
        --font-main: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    }

    /* --- Footer --- */
    .footer {
        background-color: var(--text-color);
        color: var(--bg-color);
        border-top: 2px solid var(--text-color);
        padding: 80px 0 0 0;
        font-family: var(--font-main);
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 24px;
    }

    /* --- Grid de Secciones --- */
    .footer-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 60px;
        padding-bottom: 60px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.15);
    }

    /* --- Secciones --- */
    .footer-section h3 {
        font-size: 0.8rem;
        font-weight: 700;
        color: rgba(255, 255, 255, 0.5);
        text-transform: uppercase;
        letter-spacing: 1.5px;
        margin-bottom: 20px;
    }

    /* --- Contenido --- */
    .footer-content {
        margin-top: 12px;
    }

    .contact-link {
        color: var(--bg-color);
        text-decoration: none;
        font-size: 0.95rem;
        font-weight: 500;
        transition: opacity 0.3s ease;
    }

    .contact-link:hover {
        opacity: 0.7;
    }

    /* --- Links --- */
    .footer-links {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .footer-links li {
        margin-bottom: 12px;
    }

    .footer-links a {
        color: var(--bg-color);
        text-decoration: none;
        font-size: 0.9rem;
        font-weight: 400;
        position: relative;
        transition: opacity 0.3s ease;
        display: inline-block;
    }

    .footer-links a::after {
        content: '';
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 0;
        height: 1px;
        background-color: var(--bg-color);
        transition: width 0.3s ease;
    }

    .footer-links a:hover {
        opacity: 0.7;
    }

    .footer-links a:hover::after {
        width: 100%;
    }

    /* --- Botón CTA --- */
    .footer-cta {
        display: flex;
        align-items: flex-start;
    }

    .btn-footer {
        display: inline-block;
        background-color: var(--bg-color);
        color: var(--text-color);
        padding: 12px 24px;
        text-decoration: none;
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        border: 2px solid var(--bg-color);
        transition: all 0.3s ease;
    }

    .btn-footer:hover {
        background-color: var(--text-color);
        color: var(--bg-color);
    }

    /* --- Footer Bottom --- */
    .footer-bottom {
        padding: 30px 0;
    }

    .footer-rights {
        font-size: 0.85rem;
        color: rgba(255, 255, 255, 0.4);
        text-align: center;
        letter-spacing: 0.5px;
    }

    /* --- Responsive --- */
    @media (max-width: 968px) {
        .footer {
            padding: 60px 0 0 0;
        }

        .footer-grid {
            grid-template-columns: repeat(2, 1fr);
            gap: 40px;
            padding-bottom: 40px;
        }

        .footer-cta {
            grid-column: 1 / -1;
            justify-content: center;
        }

        .btn-footer {
            width: 100%;
            text-align: center;
            max-width: 300px;
        }
    }

    @media (max-width: 576px) {
        .footer {
            padding: 40px 0 0 0;
        }

        .footer-grid {
            grid-template-columns: 1fr;
            gap: 32px;
            padding-bottom: 32px;
        }

        .footer-section {
            text-align: center;
        }

        .footer-links {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .btn-footer {
            width: 100%;
            max-width: 100%;
        }

        .footer-bottom {
            padding: 24px 0;
        }

        .footer-rights {
            font-size: 0.8rem;
        }
    }
</style>