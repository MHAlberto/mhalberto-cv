---
title: "Hero"
import perfil from '../../public/fotito.jpg';

const base = import.meta.env.BASE_URL;

const link = (path: string = '') => {
  const cleanBase = base.endsWith('/') ? base : `${base}/`;
  const cleanPath = path.replace(/^\/+/, '');
  return `${cleanBase}${cleanPath}`;
};
---

<section id="hero">
    <div class="container">
        <div class="hero-content">
            <h2>Hola, soy Mario</h2>
            <p>Desarrollador web y creador de proyectos tecnológicos. Me encanta aprender y experimentar con nuevas tecnologías.</p>
            <a href={link('contacto')} class="btn">Contáctame</a>
        </div>
        <div class="hero-image">
            <img src={perfil.src} alt="Foto de perfil" />
        </div>
    </div>
</section>

<style>
    /* --- Variables (igual que navbar) --- */
    :root {
        --bg-color: #ffffff;
        --text-color: #111111;
        --accent-color: #666666;
        --border-color: #eeeeee;
        --font-main: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    }

    /* --- Hero Section --- */
    #hero {
        background-color: var(--bg-color);
        border-bottom: 1px solid var(--border-color);
        padding: 120px 0;
        font-family: var(--font-main);
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 24px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 60px;
    }

    /* --- Contenido --- */
    .hero-content {
        flex: 1;
        max-width: 600px;
    }

    .hero-content h2 {
        font-size: 3.5rem;
        font-weight: 800;
        color: var(--text-color);
        margin-bottom: 24px;
        letter-spacing: -1px;
        line-height: 1.1;
    }

    .hero-content p {
        font-size: 1.1rem;
        color: var(--accent-color);
        margin-bottom: 40px;
        line-height: 1.7;
        font-weight: 400;
    }

    /* --- Botón (estilo navbar) --- */
    .btn {
        display: inline-block;
        background-color: var(--text-color);
        color: var(--bg-color);
        padding: 14px 32px;
        text-decoration: none;
        font-size: 0.85rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        border: 2px solid var(--text-color);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

    .btn:hover {
        background-color: var(--bg-color);
        color: var(--text-color);
    }

    /* --- Imagen --- */
    .hero-image {
        flex: 0 0 auto;
    }

    .hero-image img {
        width: 320px;
        height: 320px;
        object-fit: cover;
        border: 2px solid var(--text-color);
        transition: transform 0.3s ease;
    }

    .hero-image img:hover {
        transform: scale(1.02);
    }

    /* --- Responsive --- */
    @media (max-width: 968px) {
        #hero {
            padding: 80px 0;
        }

        .container {
            flex-direction: column;
            text-align: center;
            gap: 40px;
        }

        .hero-content {
            max-width: 100%;
        }

        .hero-content h2 {
            font-size: 2.8rem;
        }

        .hero-image img {
            width: 280px;
            height: 280px;
        }
    }

    @media (max-width: 768px) {
        #hero {
            padding: 60px 0;
        }

        .container {
            padding: 0 20px;
            gap: 32px;
        }

        .hero-content h2 {
            font-size: 2.2rem;
        }

        .hero-content p {
            font-size: 1.05rem;
            margin-bottom: 32px;
        }

        .hero-image img {
            width: 240px;
            height: 240px;
        }
    }

    @media (max-width: 576px) {
        #hero {
            padding: 50px 0;
        }

        .container {
            padding: 0 16px;
        }

        .hero-content h2 {
            font-size: 1.9rem;
            margin-bottom: 18px;
        }

        .hero-content p {
            font-size: 0.95rem;
            margin-bottom: 28px;
            line-height: 1.6;
        }

        .hero-image img {
            width: 200px;
            height: 200px;
        }

        .btn {
            width: 100%;
            text-align: center;
            padding: 12px 24px;
            font-size: 0.8rem;
        }
    }

    /* iPhone SE y dispositivos muy pequeños */
    @media (max-width: 390px) {
        #hero {
            padding: 40px 0;
        }

        .container {
            padding: 0 14px;
            gap: 24px;
        }

        .hero-content h2 {
            font-size: 1.6rem;
            margin-bottom: 14px;
        }

        .hero-content p {
            font-size: 0.9rem;
            margin-bottom: 24px;
        }

        .hero-image img {
            width: 180px;
            height: 180px;
        }

        .btn {
            padding: 11px 20px;
        }
    }
</style>